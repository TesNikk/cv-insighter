import CVForm from "@/app/components/CVForm";
import Layout from "../app/components/Layout";

const Home = () => {
  return (
    <Layout>
      <CVForm />
    </Layout>
  );
};

export default Home;
